name: Republish stranded releases (temporary)

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  function:
    uses: 7K-Group/workflows-library/.github/workflows/release-function.yml@8f3f1dd841f9f34fc74a5fdb76577119b251ec0c # v1
    with:
      path: functions/platform
      package-path: functions/platform/package
      version: '1.0.1'
      image-name: function-platform
      function-name: function-platform
    secrets:
      HARBOR_REGISTRY: ${{ secrets.HARBOR_REGISTRY }}
      HARBOR_PROJECT: ${{ secrets.HARBOR_PROJECT }}
      HARBOR_ROBOT_NAME: ${{ secrets.HARBOR_ROBOT_NAME }}
      HARBOR_ROBOT_TOKEN: ${{ secrets.HARBOR_ROBOT_TOKEN }}

  primitives:
    uses: 7K-Group/workflows-library/.github/workflows/release-crossplane.yml@8f3f1dd841f9f34fc74a5fdb76577119b251ec0c # v1
    with:
      path: packages/primitives
      version: '1.0.0'
      image: platform-primitives
      prebuild: npm run build -w @7k-hiroba/shared && npm run synth -w @7k-hiroba/primitives
    secrets:
      HARBOR_REGISTRY: ${{ secrets.HARBOR_REGISTRY }}
      HARBOR_PROJECT: ${{ secrets.HARBOR_PROJECT }}
      HARBOR_ROBOT_NAME: ${{ secrets.HARBOR_ROBOT_NAME }}
      HARBOR_ROBOT_TOKEN: ${{ secrets.HARBOR_ROBOT_TOKEN }}
