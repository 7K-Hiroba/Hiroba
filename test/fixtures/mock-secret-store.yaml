apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: platform-vault
spec:
  provider:
    fake:
      data:
        - key: platform/sso/grafana
          value: '{"client-id":"mock-client-id","client-secret":"mock-client-secret"}'
        - key: platform/alerting/pagerduty
          value: '{"api-key":"mock-pagerduty-key"}'
        - key: platform/loki/s3
          value: '{"access-key":"mock-access-key","secret-key":"mock-secret-key"}'
