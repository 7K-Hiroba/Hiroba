apiVersion: platform.yourcompany.io/v1
kind: PrometheusInstance
metadata:
  name: test-prometheus
spec:
  profile: production
  retentionDays: 90
  alerting:
    enabled: true
  federation:
    enabled: true
