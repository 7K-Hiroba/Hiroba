{
  "name": "@platform-engineering/primitives",
  "version": "0.0.0",
  "private": true,
  "description": "Aggregate Crossplane Configuration bundling all platform primitives (PostgresInstance, ObjectBucket).",
  "scripts": {
    "build": "bash aggregate.sh",
    "synth": "bash aggregate.sh",
    "package": "bash aggregate.sh && crossplane xpkg build --package-root=. -o platform-primitives.xpkg",
    "clean": "rm -f *.k8s.yaml *.xpkg"
  },
  "devDependencies": {}
}
