{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits"
  ],
  "separateMajorMinor": false,
  "separateMinorPatch": false,

  "customManagers": [
    {%- if values.upstreamImage %}
    {
      "customType": "regex",
      "description": "Track upstream ${{ values.upstreamImage }} image via Chart.yaml appVersion",
      "managerFilePatterns": ["/helm/(base|platform)/Chart\\.yaml$/"],
      "matchStrings": [
        "appVersion:\\s*\"?(?<currentValue>[^\"\\s]+)\"?"
      ],
      "depNameTemplate": "${{ values.upstreamImage }}",
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    }
    {%- endif %}
  ],

  "packageRules": [
    {
      "description": "Group Dockerfile base image updates",
      "matchManagers": ["dockerfile"],
      "groupName": "docker-base-images",
      "semanticCommitType": "fix",
      "semanticCommitScope": "docker"
    },
    {
      "description": "Group GitHub Actions updates",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions",
      "semanticCommitType": "ci"
    },
    {%- if values.upstreamImage %}
    {
      "description": "Bump both charts together when upstream ${{ values.upstreamImage }} image moves",
      "matchPackageNames": ["${{ values.upstreamImage }}"],
      "groupName": "${{ values.name }}-upstream",
      "semanticCommitType": "fix",
      "semanticCommitScope": "helm"
    },
    {%- endif %}
    {
      "description": "Platform chart dependency updates",
      "matchFileNames": ["helm/platform/**"],
      "semanticCommitType": "fix",
      "semanticCommitScope": "helm-platform"
    }
  ]
}
