apiVersion: v2
name: ${{ values.name }}
description: Helm chart for ${{ values.name }}
type: application
version: 0.1.0
appVersion: "0.1.0"
maintainers:
  - name: 7k-hiroba
keywords:
  - kubernetes
  - hiroba
home: https://github.com/7KGroup/${{ values.name }}
sources:
  - https://github.com/7KGroup/${{ values.name }}
