apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: templates
  description: Collection of all Backstage templates
spec:
  targets:
    - ./templates/app-template/template.yaml
    - ./templates/stack-template/template.yaml
