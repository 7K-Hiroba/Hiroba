{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Hiroba Documentation","text":"<p>Hiroba is 7KGroup's platform engineering framework. It delivers self-service platform products to development teams using Crossplane v2 for infrastructure orchestration and CDK8s for type-safe manifest generation. All reconciliation logic lives in a single Go orchestrator function; TypeScript packages emit XRDs and thin Pipeline Compositions only.</p> <p>:octicons-arrow-right-24: Hiroba on GitHub{ .md-button }</p>"},{"location":"#what-lives-here","title":"What lives here","text":"Section Contents Guides How to use and build on Hiroba: the Usage Guide for consuming platform products, the Developer Guide for contributing new primitives, and the Architecture overview of how the pieces fit together. Decisions (ADRs) The Architecture Decision Records behind the design \u2014 why Crossplane, why CDK8s, the orchestrator-function pattern, contract codegen, and more. Runbooks How we operate: adding providers, debugging stuck XRs, rotating credentials, upgrading compositions, and manual resource cleanup."},{"location":"#why-we-publish-this","title":"Why we publish this","text":"<p>Engineering buyers trust what they can read. Our architecture, our decisions, and our operational runbooks are public because they're the strongest evidence of how we work. If you're evaluating Hiroba, your engineers should start here.</p>"},{"location":"#related","title":"Related","text":"<ul> <li>7KGroup \u2014 company site</li> <li>Inari docs \u2014 the PTaaS offering built on Hiroba</li> <li>github.com/7K-Hiroba \u2014 Hiroba repositories</li> </ul>"},{"location":"architecture/","title":"Architecture Overview","text":"<p>Hiroba is an internal platform engineering framework built on Crossplane v2 (infrastructure orchestration) and CDK8s (type-safe manifest generation for XRDs and Compositions). All reconciliation logic lives in a single Go composition function; TypeScript packages only describe APIs.</p>"},{"location":"architecture/#the-orchestrator-adr-007","title":"The Orchestrator (ADR 007)","text":"<p><code>function-platform</code> is a single Crossplane composition function that dispatches on the composite resource's <code>apiVersion</code>/<code>kind</code>. Every product \u2014 primitive or stack \u2014 ships:</p> <ol> <li>an XRD (<code>apiextensions.crossplane.io/v2</code>, <code>scope: Namespaced</code>), and</li> <li>a thin Pipeline Composition with one step: invoke <code>function-platform</code>.</li> </ol> <p>Provider branching, profile defaults, hierarchical composition, and readiness live in the Go handlers (<code>functions/platform/handlers/</code>), not in Composition files.</p> <pre><code>packages/&lt;product&gt;/src/xrd.ts   -&gt; XRD + thin Composition (cdk8s synth)\nfunctions/platform/handlers/    -&gt; one handler per kind, registered in cmd/main.go\ncontract/contract.json          -&gt; single source of truth (ADR 009), codegen to TS + Go\n</code></pre>"},{"location":"architecture/#products","title":"Products","text":"<p>Primitives (this repo):</p> <ul> <li>PostgresInstance: <code>spec.provider</code>: <code>cnpg</code> (CloudNativePG, default) or <code>aws</code> (RDS).</li> <li>ObjectBucket: <code>spec.provider</code>: <code>garage</code> (default) or <code>aws</code> (S3).</li> </ul> <p>Observability (consumer repo, ADR 008):</p> <ul> <li>GrafanaInstance, LokiInstance, PrometheusInstance, MimirInstance,   AlloyInstance: thin XRDs reconciled by orchestrator handlers that emit   provider-helm Releases (namespaced <code>helm.m.crossplane.io</code> API).</li> <li>ObservabilityStack: emits child XRs per enabled module, wires Grafana   datasources from observed child status, and is Ready when all children are Ready.</li> </ul>"},{"location":"architecture/#crossplane-v2-contract-notes","title":"Crossplane v2 Contract Notes","text":"<ul> <li>No XR connection secrets. v2 drops <code>connectionSecretKeys</code>; function-returned   connection details for child XRs are not persisted. The public contract is   <code>status</code>: <code>phase</code>, <code>endpoint</code>, <code>connectionSecretRef</code> (pointing at the   provider-native secret, e.g. the CNPG operator's <code>&lt;name&gt;-pg-app</code>).</li> <li>Explicit readiness. Pipeline composed resources are unready unless the   function says otherwise; handlers derive readiness from the observed resource's   <code>Ready</code> condition or a healthy <code>status.phase</code>.</li> <li>Namespaced provider configs. Composed managed resources reference   <code>&lt;team&gt;-&lt;provider&gt;</code> ProviderConfigs in the XR namespace (e.g. <code>team-api-helm</code>,   <code>team-api-aws</code>).</li> </ul>"},{"location":"architecture/#dependency-gate","title":"Dependency Gate","text":"<p>The orchestrator fails fast when a required operator is missing. <code>contract/contract.json</code> declares required CRDs per XR kind and provider (<code>dependencies</code>); before dispatching to a handler, the function verifies those CRDs are installed (discovery API, 60s cache). A missing dependency aborts the reconcile with a fatal, client-actionable error naming the CRDs and the operator to install \u2014 installing operators stays the platform operator's responsibility, and the error is visible via <code>kubectl describe &lt;xr&gt;</code>. Outside a cluster (render, tests) the check is a no-op.</p>"},{"location":"architecture/#provider-profile-defaults","title":"Provider &amp; Profile Defaults","text":"<p>Precedence: <code>spec.*</code> override &gt; function env config (<code>PLATFORM_DEFAULT_*</code>) &gt; contract default. Profile (<code>development|staging|production</code>) supplies defaults for instance class, HA, backups, and deletion policy per the contract.</p>"},{"location":"architecture/#repository-layout","title":"Repository Layout","text":"<pre><code>hiroba/\n\u251c\u2500\u2500 contract/          # contract.json + codegen\n\u251c\u2500\u2500 functions/platform/# Go orchestrator (handlers, registry, provider config)\n\u251c\u2500\u2500 packages/          # shared lib + primitives (XRDs via cdk8s)\n\u251c\u2500\u2500 consumer-sdk/      # Developer-facing typed constructs\n\u251c\u2500\u2500 infrastructure/    # Control-plane manifests (providers)\n\u251c\u2500\u2500 scripts/           # e2e-setup.sh, team-setup.sh, render-validate.sh\n\u251c\u2500\u2500 examples/          # Example consumer projects\n\u2514\u2500\u2500 docs/              # Documentation + ADRs\n</code></pre>"},{"location":"architecture/#design-principles","title":"Design Principles","text":"<ol> <li>One reconciliation brain: all logic in the orchestrator; TS describes APIs only.</li> <li>Status is the contract: deterministic names + status fields, not secrets.</li> <li>Profile-driven defaults: development/staging/production encode sensible defaults.</li> <li>Escape hatch: every Helm-backed module accepts <code>spec.values</code> (chart defaults &lt;    user values &lt; platform wiring).</li> <li>Schema-first guardrails: XRD schemas enforce structure; the contract codegen    keeps TS and Go in lockstep.</li> </ol>"},{"location":"developer-guide/","title":"Developer Guide","text":""},{"location":"developer-guide/#quick-start","title":"Quick Start","text":""},{"location":"developer-guide/#1-install-dependencies","title":"1. Install Dependencies","text":"<pre><code>make install\n</code></pre>"},{"location":"developer-guide/#2-build-and-test","title":"2. Build and Test","text":"<pre><code>make lint\nmake test\nmake synth\nmake validate\n</code></pre>"},{"location":"developer-guide/#3-consume-the-platform","title":"3. Consume the Platform","text":"<pre><code>cd examples/team-api\nnpm install\nnpm run synth\nkubectl apply -f dist/obs.k8s.yaml\n</code></pre>"},{"location":"developer-guide/#documentation","title":"Documentation","text":"<ul> <li>Usage Guide \u2014 detailed guide for consumers and platform engineers</li> <li>Architecture</li> </ul>"},{"location":"developer-guide/#adding-a-new-primitive-product","title":"Adding a New Primitive Product","text":"<p>For a step-by-step walkthrough, see the Usage Guide. In short:</p> <ol> <li>Use <code>npx create-platform-product</code> or create a new package under <code>packages/&lt;product&gt;/</code>.</li> <li>Add <code>cdk8s.yaml</code> with required CRD imports.</li> <li>Run <code>npx cdk8s import</code> to generate typed constructs.</li> <li>Define the XRD in <code>src/xrd.ts</code>.</li> <li>Define the Composition in <code>src/composition.ts</code>.</li> <li>Add unit tests in <code>test/unit/</code>.</li> <li>Add packaging metadata in <code>package/crossplane.yaml</code>.</li> </ol>"},{"location":"developer-guide/#profile-defaults","title":"Profile Defaults","text":"Profile Grafana Replicas Storage Loki Storage Loki Replication development 1 10Gi local 1 staging 1 20Gi s3 2 production 2 50Gi s3 3"},{"location":"developer-guide/#feature-toggles","title":"Feature Toggles","text":"Feature GrafanaInstance LokiInstance PrometheusInstance ObservabilityStack SSO yes no no global Alerting yes no yes global Ingress yes no no no Federation no no yes no"},{"location":"developer-guide/#troubleshooting","title":"Troubleshooting","text":""},{"location":"developer-guide/#make-synth-fails-with-cannot-find-a-parent-chart","title":"<code>make synth</code> fails with \"cannot find a parent chart\"","text":"<p>Ensure XRD and Composition classes extend <code>Chart</code> from <code>cdk8s</code>.</p>"},{"location":"developer-guide/#make-validate-fails-with-string-transform-error","title":"<code>make validate</code> fails with string transform error","text":"<p>Crossplane v2 requires <code>string.type: Format</code>. Use the <code>transformString</code> helper from <code>@7k-hiroba/shared</code>.</p>"},{"location":"usage-guide/","title":"Platform Engineering Usage Guide","text":"<p>This guide explains how to consume platform products, create new applications, and build new products on the orchestrator architecture (ADR 007).</p>"},{"location":"usage-guide/#table-of-contents","title":"Table of Contents","text":"<ol> <li>Concepts</li> <li>Quick Start</li> <li>Using Existing Platform Products</li> <li>Creating a New Application</li> <li>Creating a New Product Package</li> <li>Validation and Deployment</li> <li>Testing Patterns</li> </ol>"},{"location":"usage-guide/#concepts","title":"Concepts","text":"<p>Infrastructure is modeled as platform products that teams request as namespaced Kubernetes resources (Crossplane v2 XRs \u2014 there is no separate Claim model).</p> Term Description Primitive Product A single reusable infrastructure abstraction (<code>PostgresInstance</code>, <code>ObjectBucket</code>). Stack A composite product orchestrating multiple children (<code>ObservabilityStack</code>). XR The namespaced composite resource a developer creates to request a product. XRD CompositeResourceDefinition (<code>apiextensions.crossplane.io/v2</code>). Defines the XR schema. Composition Thin Pipeline composition delegating to the orchestrator function. Orchestrator <code>function-platform</code>: the Go composition function that reconciles every kind. Profile Environment tier: <code>development</code>, <code>staging</code>, or <code>production</code>. Drives contract defaults. Feature Toggle Optional capability under <code>spec.features.&lt;name&gt;.enabled</code> (e.g. HA, backups)."},{"location":"usage-guide/#quick-start","title":"Quick Start","text":""},{"location":"usage-guide/#1-bootstrap-a-local-cluster","title":"1. Bootstrap a local cluster","text":"<pre><code>scripts/e2e-setup.sh platform-e2e        # Crossplane, providers, ESO, CNPG, provider-helm, function-platform\nscripts/team-setup.sh team-api           # per-team namespace + helm ProviderConfig + RoleBinding\n</code></pre>"},{"location":"usage-guide/#2-install-product-apis","title":"2. Install product APIs","text":"<pre><code>kubectl --context kind-platform-e2e apply \\\n  -f packages/postgres/dist/xrd.k8s.yaml \\\n  -f packages/postgres/dist/composition.k8s.yaml\n</code></pre>"},{"location":"usage-guide/#3-develop","title":"3. Develop","text":"<pre><code>bash scripts/pre-push-checks.sh   # contract check, lint, build, tests, synth, validate, go vet/test -race\nnpm run gen:contract              # regenerate TS + Go after editing contract/contract.json\n</code></pre>"},{"location":"usage-guide/#using-existing-platform-products","title":"Using Existing Platform Products","text":""},{"location":"usage-guide/#with-kubectl","title":"With kubectl","text":"<pre><code>apiVersion: platform.7kgroup.org/v1alpha1\nkind: PostgresInstance\nmetadata:\n  name: team-api-db\n  namespace: team-api\nspec:\n  team: team-api\n  profile: development\n  costCenter: cc-12345\n  features:\n    ha:\n      enabled: true\n</code></pre> <p>Status is the contract (Crossplane v2 has no XR connection secrets):</p> <pre><code>kubectl -n team-api get postgresinstance team-api-db \\\n  -o jsonpath='{.status.endpoint}{\"\\n\"}{.status.phase}{\"\\n\"}{.status.connectionSecretRef.name}{\"\\n\"}'\n# team-api-db-pg-rw.team-api.svc:5432\n# Ready\n# team-api-db-pg-app          &lt;- CNPG operator secret (keys: username, password, dbname, host, port, uri)\n</code></pre>"},{"location":"usage-guide/#with-the-typescript-consumer-sdk","title":"With the TypeScript Consumer SDK","text":"<pre><code>import { App } from 'cdk8s';\nimport { TeamObservability, PostgresInstance } from '@7k-hiroba/platform-consumer';\n\nconst app = new App({ outdir: 'dist' });\n\nnew PostgresInstance(app, 'db', {\n  name: 'team-api-db',\n  namespace: 'team-api',\n  team: 'team-api',\n  profile: 'development',\n  costCenter: 'cc-12345',\n});\n\nnew TeamObservability(app, 'obs', {\n  name: 'team-api-obs',\n  namespace: 'team-api',\n  team: 'team-api',\n  profile: 'development',\n  modules: {\n    grafana: { enabled: true, domain: 'grafana.team-api.example.com' },\n    loki: { enabled: false },\n    metrics: { enabled: true, backend: 'prometheus' },\n    alloy: { enabled: true },\n  },\n});\n\napp.synth();\n</code></pre> <pre><code>npx ts-node src/main.ts &amp;&amp; kubectl apply -f dist/\n</code></pre> <p>For products without a dedicated construct, use the generic <code>PlatformXr</code> construct from the SDK.</p>"},{"location":"usage-guide/#creating-a-new-application","title":"Creating a New Application","text":"<p>An application repository consumes products through the SDK:</p> <pre><code>mkdir examples/my-service &amp;&amp; cd examples/my-service\nnpm init -y\nnpm install cdk8s @7k-hiroba/platform-consumer ts-node typescript\n</code></pre> <p>Write <code>src/main.ts</code> (see the SDK example above), then:</p> <pre><code>npx ts-node src/main.ts\nkubectl apply -f dist/\n</code></pre>"},{"location":"usage-guide/#creating-a-new-product-package","title":"Creating a New Product Package","text":"<p>Products ship an XRD + thin Composition from a cdk8s package, plus a Go handler in the orchestrator.</p>"},{"location":"usage-guide/#1-scaffold-the-package","title":"1. Scaffold the package","text":"<pre><code>npx create-platform-product --name redis --category database\n</code></pre>"},{"location":"usage-guide/#2-define-the-xrd","title":"2. Define the XRD","text":"<pre><code>import { Chart } from 'cdk8s';\nimport { Construct } from 'constructs';\nimport { createPlatformXrd, createBaseSchema, API_GROUP, API_VERSION } from '@7k-hiroba/shared';\n\nexport class RedisInstanceXrd extends Chart {\n  constructor(scope: Construct, id: string) {\n    super(scope, id);\n    createPlatformXrd(\n      this,\n      'xrd',\n      {\n        group: API_GROUP,\n        version: API_VERSION, // v1alpha1\n        kind: 'RedisInstance',\n        plural: 'redisinstances',\n        singular: 'redisinstance',\n        scope: 'Namespaced',\n      },\n      {\n        ...createBaseSchema(),\n        provider: { type: 'string', enum: ['aws', 'incluster'] },\n      },\n      ['profile', 'team', 'costCenter'],\n    );\n  }\n}\n</code></pre> <p>Schema rules imposed by the v2 XRD -&gt; CRD converter:</p> <ul> <li>no <code>connectionSecretKeys</code> (XR connection secrets do not exist in v2);</li> <li>no <code>additionalProperties</code> combined with <code>properties</code> on nested object fields \u2014   keep nested objects either structural (<code>properties</code> only) or free-form.</li> </ul>"},{"location":"usage-guide/#3-emit-the-thin-composition","title":"3. Emit the thin Composition","text":"<pre><code>import { createOrchestratedComposition } from '@7k-hiroba/shared';\n// one Pipeline step invoking function-platform; no composition logic in TS\n</code></pre>"},{"location":"usage-guide/#4-implement-the-go-handler","title":"4. Implement the Go handler","text":"<p>Add <code>functions/platform/handlers/redis.go</code>:</p> <ul> <li>read the observed XR (<code>hc.OXR</code>), resolve provider   (<code>spec.provider</code> &gt; <code>PLATFORM_DEFAULT_*</code> env &gt; contract default);</li> <li>emit desired composed resources with deterministic names;</li> <li>set explicit readiness: <code>platform.MarkReady(res, hc, name)</code>;</li> <li>publish <code>status.phase</code> / <code>status.endpoint</code> / <code>status.connectionSecretRef</code>;</li> <li>for Helm workloads use the namespaced <code>helm.m.crossplane.io/v1beta1</code> Release API   and <code>platform.ResolveProviderConfig(oxr, \"helm\")</code> (<code>&lt;team&gt;-helm</code>);</li> <li>wire children from observed child <code>status</code> \u2014 never from connection details   (v2 drops XR-level connection secrets).</li> </ul> <p>Register the handler in <code>cmd/main.go</code> and add tests in <code>handlers/handlers_test.go</code>.</p>"},{"location":"usage-guide/#5-update-the-contract","title":"5. Update the contract","text":"<p>Add the product to <code>contract/contract.json</code> (kind, plural, providers, profiles, connection keys) and declare its operator dependencies so missing operators fail fast with an actionable error:</p> <pre><code>\"dependencies\": {\n  \"RedisInstance\": {\n    \"*\": [{ \"crd\": \"releases.helm.m.crossplane.io\", \"hint\": \"install provider-helm\" }],\n    \"aws\": [{ \"crd\": \"instances.elasticache.aws.m.upbound.io\", \"hint\": \"install provider-aws-elasticache\" }]\n  }\n}\n</code></pre> <p>Run <code>npm run gen:contract</code>. The <code>--check</code> gate in <code>scripts/pre-push-checks.sh</code> enforces freshness.</p>"},{"location":"usage-guide/#validation-and-deployment","title":"Validation and Deployment","text":""},{"location":"usage-guide/#local-render-validation","title":"Local render validation","text":"<pre><code>HIROBA_ROOT=$PWD bash scripts/render-validate.sh packages/postgres\n</code></pre>"},{"location":"usage-guide/#deploy-to-kubernetes","title":"Deploy to Kubernetes","text":"<pre><code>kubectl apply -f packages/postgres/dist/xrd.k8s.yaml\nkubectl apply -f packages/postgres/dist/composition.k8s.yaml\n</code></pre> <p>For Helm-backed products, each team namespace needs (created by <code>scripts/team-setup.sh</code>): a namespaced <code>ProviderConfig</code> <code>&lt;team&gt;-helm</code> (<code>helm.m.crossplane.io/v1beta1</code>) and a RoleBinding granting the provider-helm service account <code>admin</code> in the namespace.</p>"},{"location":"usage-guide/#live-e2e-on-kind","title":"Live e2e on kind","text":"<pre><code>scripts/e2e-setup.sh platform-e2e\nscripts/team-setup.sh team-api\n# apply XRDs/Compositions, create an XR, assert Ready + status.endpoint\n</code></pre>"},{"location":"usage-guide/#testing-patterns","title":"Testing Patterns","text":"<ul> <li>TS packages: synthesize the chart and assert the XRD/Composition shape   (<code>Testing.synth</code>). See <code>packages/postgres/test/unit/snapshot.test.ts</code>.</li> <li>Go handlers: table tests building a <code>HandlerContext</code> with observed composed   resources; assert desired resources, readiness, and status. See   <code>functions/platform/handlers/handlers_test.go</code>.</li> <li>Render: <code>scripts/render-validate.sh</code> runs <code>crossplane composition render</code>   against fixtures.</li> </ul> <pre><code>npm run test:unit --workspace=@7k-hiroba/postgres\n(cd functions/platform &amp;&amp; go test -race ./...)\n</code></pre>"},{"location":"adr/001-why-crossplane/","title":"ADR 001: Why Crossplane","text":""},{"location":"adr/001-why-crossplane/#status","title":"Status","text":"<p>Accepted</p>"},{"location":"adr/001-why-crossplane/#context","title":"Context","text":"<p>We need a control plane to orchestrate infrastructure across multiple Kubernetes clusters and cloud providers.</p>"},{"location":"adr/001-why-crossplane/#decision","title":"Decision","text":"<p>Use Crossplane v2 as the universal control plane.</p>"},{"location":"adr/001-why-crossplane/#consequences","title":"Consequences","text":"<ul> <li>Enables declarative infrastructure as Kubernetes resources.</li> <li>Supports composition of higher-level abstractions (XRDs).</li> <li>Requires investment in provider management and function pipelines.</li> </ul>"},{"location":"adr/002-why-cdk8s/","title":"ADR 002: Why CDK8s","text":""},{"location":"adr/002-why-cdk8s/#status","title":"Status","text":"<p>Accepted</p>"},{"location":"adr/002-why-cdk8s/#context","title":"Context","text":"<p>Platform manifests (XRDs, Compositions) must be maintainable, type-safe, and testable.</p>"},{"location":"adr/002-why-cdk8s/#decision","title":"Decision","text":"<p>Use CDK8s with TypeScript for manifest generation.</p>"},{"location":"adr/002-why-cdk8s/#consequences","title":"Consequences","text":"<ul> <li>Type safety and IDE support.</li> <li>Reusable constructs and snapshot testing.</li> <li>Additional build step before deploying YAML.</li> </ul>"},{"location":"adr/003-secret-management-strategy/","title":"ADR 003: Secret Management Strategy","text":""},{"location":"adr/003-secret-management-strategy/#status","title":"Status","text":"<p>Accepted</p>"},{"location":"adr/003-secret-management-strategy/#context","title":"Context","text":"<p>Platform products need credentials for SSO, cloud providers, and alerting integrations.</p>"},{"location":"adr/003-secret-management-strategy/#decision","title":"Decision","text":"<p>Use External Secrets Operator (ESO) with HashiCorp Vault as the primary backend, with native Kubernetes Secrets as a fallback.</p>"},{"location":"adr/003-secret-management-strategy/#consequences","title":"Consequences","text":"<ul> <li>Centralized secret lifecycle management.</li> <li>No secrets stored in Git.</li> <li>Requires ESO and Vault to be operational.</li> </ul>"},{"location":"adr/004-claims-vs-xrs/","title":"ADR 004: Claims vs XRs","text":""},{"location":"adr/004-claims-vs-xrs/#status","title":"Status","text":"<p>Superseded by ADR 007 \u2014 Crossplane v2 uses namespaced XRDs; the Claim model no longer exists and consumers create XRs directly in their namespaces.</p>"},{"location":"adr/004-claims-vs-xrs/#context","title":"Context","text":"<p>Developers need namespace-scoped access to platform resources.</p>"},{"location":"adr/004-claims-vs-xrs/#decision","title":"Decision","text":"<p>Expose namespace-scoped Claims (e.g., <code>GrafanaInstanceClaim</code>) that bind to cluster-scoped Composite Resources (XRs).</p>"},{"location":"adr/004-claims-vs-xrs/#consequences","title":"Consequences","text":"<ul> <li>Teams can create resources in their own namespaces.</li> <li>Platform team retains control over XR lifecycle and policies.</li> <li>Claims simplify RBAC and multi-tenancy.</li> </ul>"},{"location":"adr/005-feature-toggle-pattern/","title":"ADR 005: Feature Toggle Pattern","text":""},{"location":"adr/005-feature-toggle-pattern/#status","title":"Status","text":"<p>Accepted</p>"},{"location":"adr/005-feature-toggle-pattern/#context","title":"Context","text":"<p>Platform products need optional features (SSO, alerting, ingress) without duplicating Compositions.</p>"},{"location":"adr/005-feature-toggle-pattern/#decision","title":"Decision","text":"<p>Use boolean feature toggles in the XRD schema and patch-based conditional resource creation via <code>function-patch-and-transform</code>. Future iterations may migrate to custom Composition Functions.</p>"},{"location":"adr/005-feature-toggle-pattern/#consequences","title":"Consequences","text":"<ul> <li>Single Composition supports multiple configurations.</li> <li>Patch-based toggles set <code>replicas: 0</code> for disabled resources.</li> <li>Composition Functions enable true conditional creation in Phase 6.</li> </ul>"},{"location":"adr/006-multi-provider-compositions/","title":"ADR 006: Multi-Provider Compositions","text":""},{"location":"adr/006-multi-provider-compositions/#status","title":"Status","text":"<p>Accepted</p>"},{"location":"adr/006-multi-provider-compositions/#context","title":"Context","text":"<p>The Hiroba catalog needs to support multiple infrastructure backends for the same platform product. Clients may run on AWS, Azure, or GCP, and some may prefer in-cluster operators (Garage for object storage, CNPG for Postgres) over managed cloud services.</p> <p>The Inari architecture defines the catalog layout as:</p> <pre><code>hiroba-catalog/\n\u251c\u2500\u2500 xrd/\n\u251c\u2500\u2500 compositions/\n\u2502   \u251c\u2500\u2500 aws/\n\u2502   \u251c\u2500\u2500 gcp/\n\u2502   \u251c\u2500\u2500 azure/\n\u2502   \u2514\u2500\u2500 common/\n\u2514\u2500\u2500 profiles/\n</code></pre> <p>We evaluated three alternatives:</p> <ol> <li>Capability-discovery orchestrator function \u2014 a Crossplane Function that discovers cluster capabilities and dynamically selects backends.</li> <li>Kratix \u2014 an open-source platform framework based on Promises and workflow pipelines.</li> <li>Provider-per-composition \u2014 one Crossplane Composition per product per backend, selected via <code>spec.provider</code> or composition labels.</li> </ol>"},{"location":"adr/006-multi-provider-compositions/#decision","title":"Decision","text":"<p>Use provider-per-composition (option 3). Each product XRD remains cloud-agnostic, and each backend is implemented as a separate Composition. Operator backends (Garage, CNPG) are treated as additional providers.</p>"},{"location":"adr/006-multi-provider-compositions/#consequences","title":"Consequences","text":"<ul> <li>Aligns directly with the Inari catalog structure (ADR-003).</li> <li>No new runtime components (no orchestrator function, no capability agent).</li> <li>Adding a new backend means adding a new composition directory, which is predictable and testable.</li> <li>Consumers may optionally specify <code>spec.provider</code>; otherwise the cluster default applies.</li> <li>Consumers may optionally specify <code>spec.providerConfigRef.name</code> and <code>spec.region</code> to control the backing provider configuration and AWS region; when omitted, the composition uses cluster defaults.</li> <li>Provider-specific logic is isolated, making it easier to maintain and debug.</li> </ul>"},{"location":"adr/006-multi-provider-compositions/#related","title":"Related","text":"<ul> <li>ADR 003: Hiroba Framework (Inari docs)</li> <li>ADR 001: Why Crossplane</li> </ul>"},{"location":"adr/007-orchestrator-function/","title":"ADR 007: Central Orchestrator Function &amp; Crossplane v2-Only","text":""},{"location":"adr/007-orchestrator-function/#status","title":"Status","text":"<p>Accepted. Supersedes ADR 006: Multi-Provider Compositions.</p>"},{"location":"adr/007-orchestrator-function/#context","title":"Context","text":"<p>Hiroba is being reshaped around a small set of agnostic, reusable primitives (e.g. <code>PostgresInstance</code>, <code>ObjectBucket</code>) that any stack may compose, each with an inherent backend toggle (<code>spec.provider</code>: <code>aws</code>, <code>gcp</code>, <code>azure</code>, <code>garage</code>, <code>cnpg</code>, <code>s3</code>, <code>local</code>). Nothing in the catalog has been published as a product yet, so we are free to remove code rather than migrate it.</p> <p>ADR 006 implemented the provider toggle as one Composition per backend (<code>compositions/aws.ts</code>, <code>compositions/cnpg.ts</code>, ...) selected by <code>spec.provider</code> via <code>compositionSelector</code>. That approach:</p> <ul> <li>duplicates Composition scaffolding per backend;</li> <li>cannot cleanly compose a primitive inside a stack (patch-and-transform does not   orchestrate child XRs well);</li> <li>still embeds provider branching in generated YAML rather than in testable code.</li> </ul> <p>We also decided to standardize on Crossplane v2 exclusively: namespaced composite resources (no legacy Claim model), v2 XRDs, and (optionally) namespaced providers for per-team credential isolation.</p>"},{"location":"adr/007-orchestrator-function/#decision","title":"Decision","text":"<ol> <li>Single central orchestrator Composition Function (<code>function-platform</code>), written    in Go with <code>function-sdk-go</code>. Every Composition \u2014 primitives and stacks \u2014 runs in    <code>Pipeline</code> mode with one step that calls <code>function-platform</code>. The function dispatches    on the observed composite's <code>apiVersion/kind</code> via an internal registry and emits the    desired composed resources for that kind.</li> <li>Provider branching lives in the function, not in per-backend Composition files.    A primitive handler reads <code>spec.provider</code> and emits the matching managed resource(s)    (e.g. RDS <code>Instance</code> for <code>aws</code>, CNPG <code>Cluster</code> for <code>cnpg</code>).</li> <li>Hierarchical composition: a stack handler emits child primitive XRs as composed    resources; those child XRs are reconciled by their own Composition (also calling    <code>function-platform</code>), which emits the managed resources. One binary orchestrates the    whole stack \u2192 primitive \u2192 managed tree.</li> <li>Crossplane v2 only. All XRDs use <code>apiextensions.crossplane.io/v2</code> with    <code>spec.scope: Namespaced</code>; the legacy <code>claimNames</code> / cluster-scoped Claim model is not    used. Consumers create the XR directly in their namespace.</li> <li>Namespaced providers (<code>pkg.m.crossplane.io</code>) for per-team credential isolation.    The function resolves <code>spec.providerConfigRef</code> to a namespace-scoped <code>ProviderConfig</code>    (convention <code>&lt;team&gt;-&lt;provider&gt;</code>, overridable on the XR).</li> <li>A stable connection-secret contract is defined per primitive (see    <code>packages/shared</code>) so stacks can later reference a primitive's connection details    instead of inlining infrastructure.</li> </ol>"},{"location":"adr/007-orchestrator-function/#consequences","title":"Consequences","text":"<ul> <li>One Composition per product (not per backend); adding a backend is a branch in a   handler, not a new Composition file.</li> <li>Shared concerns (provider \u2192 region / <code>providerConfigRef</code> mapping, mandatory labels,   naming, connection-secret aggregation) live once in the function.</li> <li>Introduces a Go toolchain into a previously TypeScript-only repository: <code>go.mod</code>,   Dockerfile, function-image/xpkg build and a CI lane.</li> <li>ADR 006's per-provider Composition file layout is retired.</li> <li>All existing v1 XRDs and <code>*Claim</code> usage are removed/rewritten as part of this change.</li> </ul>"},{"location":"adr/007-orchestrator-function/#v2-only-delete-checklist-completed-2026-07-12","title":"v2-only / delete checklist (completed 2026-07-12)","text":"<ul> <li>[x] All XRDs emit <code>apiextensions.crossplane.io/v2</code> with <code>spec.scope: Namespaced</code>.</li> <li>[x] No <code>claimNames</code> on any XRD; no <code>*Claim</code> kinds emitted by the consumer SDK.</li> <li>[x] CI lint gate rejects <code>apiextensions.crossplane.io/v1</code> XRDs and any <code>claimNames</code>.</li> <li>[x] Remove per-provider Composition file split (<code>compositions/{aws,cnpg,garage,...}.ts</code>).</li> <li>[x] Remove inline RDS/CNPG from <code>grafana</code> and inline S3/Garage from <code>loki</code>/<code>mimir</code>;       replaced by child <code>PostgresInstance</code> / <code>ObjectBucket</code> XRs emitted by the handlers.</li> <li>[x] Remove dead <code>functions/grafana-sso/*</code> remnants.</li> <li>[x] Remove deprecated <code>CloudProvider</code> / <code>MultiCloudResourceConfig</code> aliases in       <code>packages/shared/src/types.ts</code>.</li> <li>[x] Collapse the duplicated <code>packages/shared</code> in the Observability Stack repo; consume       Hiroba's <code>@7k-hiroba/shared</code> as the single source of truth.</li> </ul>"},{"location":"adr/007-orchestrator-function/#v2-conformance-notes-added-2026-07-12-live-e2e-on-kind","title":"v2 conformance notes (added 2026-07-12, live e2e on kind)","text":"<ul> <li>Crossplane v2 drops XR-level connection secrets: <code>connectionSecretKeys</code> is not   supported on v2 XRDs, and function-returned connection details for child XRs are   not persisted. The contract surface is <code>status</code> (endpoint, phase,   connectionSecretRef); cross-child wiring reads the child XR's <code>status</code>, not   connection details.</li> <li>The v2 XRD -&gt; CRD converter rejects <code>additionalProperties</code> combined with   <code>properties</code> on nested object fields; keep nested schemas structural or free-form.</li> <li>Namespaced provider APIs (<code>*.m.*</code>, e.g. <code>helm.m.crossplane.io/v1beta1</code>,   <code>*.m.upbound.io/v1beta1</code>) require <code>spec.providerConfigRef.kind</code>; legacy   cluster-scoped APIs forbid it. <code>SetProviderConfigRef</code> branches on the composed   resource's apiVersion.</li> <li>Function pipeline readiness is explicit: composed resources without a readiness   verdict are treated as unready. Handlers set readiness from the observed   resource's <code>Ready</code> condition or a healthy <code>status.phase</code>.</li> <li>The function container reads mTLS certs from <code>/tls/server</code> (Crossplane v2 mount).</li> </ul>"},{"location":"adr/007-orchestrator-function/#related","title":"Related","text":"<ul> <li>ADR 006: Multi-Provider Compositions (superseded)</li> <li>ADR 005: Feature Toggle Pattern</li> <li>ADR 001: Why Crossplane</li> </ul>"},{"location":"adr/008-stack-composition-in-orchestrator/","title":"ADR 008: Product Stacks Compose Inside the Orchestrator","text":""},{"location":"adr/008-stack-composition-in-orchestrator/#status","title":"Status","text":"<p>Accepted.</p>"},{"location":"adr/008-stack-composition-in-orchestrator/#context","title":"Context","text":"<p>The consumer \"Observability Stack\" repo originally shipped a full cdk8s/TypeScript composition framework: per-module Composition files, a wiring layer that stitched modules together, and vendored copies of Hiroba's <code>packages/shared</code>. With ADR 007, Hiroba's TS packages already degenerated to XRD + thin Pipeline Composition; keeping a parallel composition engine in the consumer repo duplicated the orchestrator's job and drifted from the shared library (the vendored fork was ~170k LOC).</p>"},{"location":"adr/008-stack-composition-in-orchestrator/#decision","title":"Decision","text":"<p>Product stacks (<code>ObservabilityStack</code>) are composed inside the Go orchestrator, exactly like primitives:</p> <ol> <li>The consumer repo's packages emit only the XRD (<code>createPlatformXrd</code>) and a thin    orchestrated Composition (<code>createOrchestratedComposition</code>). No composition logic    lives in TypeScript.</li> <li><code>function-platform</code> registers a handler per stack kind    (<code>ObservabilityStack</code>, ...). The stack handler emits child XRs    (<code>GrafanaInstance</code>, <code>LokiInstance</code>, <code>PrometheusInstance</code>/<code>MimirInstance</code>,    <code>AlloyInstance</code>) with deterministic names (<code>&lt;stack&gt;-&lt;module&gt;</code>), forwarding    profile/team/costCenter and the per-module <code>spec.modules.&lt;name&gt;.values</code> escape    hatch. Child XRs reconcile through their own thin Compositions back into the same    orchestrator (hierarchical composition).</li> <li>Cross-child wiring (Grafana datasources) is emitted by the stack handler as a    ConfigMap labeled <code>grafana_datasource: 1</code>, built from observed child <code>status</code>    (Crossplane v2 persists no XR-level connection details).</li> <li>The stack is Ready when every enabled child is Ready; <code>status.endpoint</code> points at    the Grafana service.</li> </ol>"},{"location":"adr/008-stack-composition-in-orchestrator/#consequences","title":"Consequences","text":"<ul> <li>One reconciliation brain for the whole platform; consumer repos need no Go or   composition code, only XRD schemas and tests.</li> <li>Deleting the consumer composition framework removed ~170k LOC of dead code and the   vendored shared fork; the consumer now depends on <code>@7k-hiroba/shared</code> directly.</li> <li>Stack handlers must be registered in <code>cmd/main.go</code>; adding a module to a stack is   a Go change in Hiroba, not a consumer-repo change. The values escape hatch keeps   chart-level customization in team hands.</li> <li>Crossplane v2 prunes child XRs automatically when a module is disabled (the   handler drops them from desired state).</li> </ul>"},{"location":"adr/009-contract-codegen/","title":"ADR 009: Contract Codegen From a Single JSON Source","text":""},{"location":"adr/009-contract-codegen/#status","title":"Status","text":"<p>Accepted.</p>"},{"location":"adr/009-contract-codegen/#context","title":"Context","text":"<p>The platform contract \u2014 API group/version, product kinds and plurals, profile defaults per product, supported providers and defaults, connection key names \u2014 was defined independently in TypeScript (<code>packages/shared</code>) and Go (<code>functions/platform</code>). The two copies drifted silently; a provider added to the TS enum but not the Go handler (or vice versa) only surfaced at runtime.</p>"},{"location":"adr/009-contract-codegen/#decision","title":"Decision","text":"<p><code>contract/contract.json</code> is the single source of truth. <code>npm run gen:contract</code> (<code>scripts/gen-contract.ts</code>) generates:</p> <ul> <li><code>packages/shared/src/contract.gen.ts</code> \u2014 consumed by the cdk8s packages and the   consumer SDK;</li> <li><code>functions/platform/internal/contract/contract.gen.go</code> \u2014 consumed by the   orchestrator handlers.</li> </ul> <p>Rules:</p> <ul> <li>Generated files carry a <code>// Code generated ... DO NOT EDIT</code> header and are never   hand-edited.</li> <li>The Go output is passed through <code>gofmt</code> inside the generator so generated code   matches gofmt'ed trees (otherwise <code>--check</code> oscillates).</li> <li><code>npm run gen:contract -- --check</code> regenerates in memory and fails on drift; it   runs in <code>scripts/pre-push-checks.sh</code> and CI, so a contract change without codegen   cannot be committed.</li> </ul>"},{"location":"adr/009-contract-codegen/#consequences","title":"Consequences","text":"<ul> <li>Adding a provider/profile/product key is a one-file change plus regeneration;   both languages stay consistent by construction.</li> <li>The generator itself is small (~200 LOC) and has no third-party dependencies   beyond ts-node and gofmt.</li> <li>Contract values that are environment-specific (cluster default provider) do NOT   belong in the contract; they come from the function's env config   (<code>PLATFORM_DEFAULT_*</code>), with the contract supplying the fallback default.</li> </ul>"},{"location":"runbooks/adding-a-new-provider/","title":"Adding a New Provider to a Hiroba Product","text":"<p>This runbook describes how to add a new infrastructure backend (provider) to an existing Hiroba platform product.</p>"},{"location":"runbooks/adding-a-new-provider/#overview","title":"Overview","text":"<p>Hiroba uses a provider-per-composition pattern:</p> <ul> <li>One XRD defines the cloud-agnostic consumer API.</li> <li>One Composition implements the product for each supported provider.</li> <li>Compositions are selected via <code>spec.provider</code> + <code>compositionSelector</code>.</li> </ul>"},{"location":"runbooks/adding-a-new-provider/#steps","title":"Steps","text":""},{"location":"runbooks/adding-a-new-provider/#1-add-the-provider-to-shared-types","title":"1. Add the provider to shared types","text":"<p>Edit <code>packages/shared/src/types.ts</code>:</p> <pre><code>export type InfrastructureProvider = 'aws' | 'gcp' | 'azure' | 'garage' | 'cnpg' | 'myprovider';\n</code></pre> <p>Also update <code>StorageBackendType</code> if the provider introduces a new storage type.</p>"},{"location":"runbooks/adding-a-new-provider/#2-update-the-product-xrd","title":"2. Update the product XRD","text":"<p>Edit <code>packages/&lt;product&gt;/src/xrd.ts</code>:</p> <ul> <li>Add the new provider value to <code>spec.provider</code> enum.</li> <li>Add per-module provider overrides if applicable.</li> </ul>"},{"location":"runbooks/adding-a-new-provider/#3-create-the-provider-specific-composition","title":"3. Create the provider-specific composition","text":"<p>Create <code>packages/&lt;product&gt;/src/compositions/&lt;myprovider&gt;.ts</code>:</p> <ul> <li>Import provider-specific CRDs (e.g., managed resources or operator CRDs).</li> <li>Add the CRD URL to <code>cdk8s.yaml</code> and run <code>npm run import</code> to generate typed constructs.</li> <li>Use <code>SomeResourceV1Beta1.manifest({ ... })</code> as the <code>base</code> of Crossplane patch-and-transform resources.</li> <li>Emit the concrete resources needed for the backend.</li> <li>Normalize connection secrets so downstream consumers see a stable shape.</li> <li>Use <code>createProviderCompositionName()</code> and <code>createProviderCompositionLabels()</code> from <code>@7k-hiroba/shared</code>.</li> <li>Patch <code>spec.providerConfigRef.name</code> and <code>spec.region</code> (for cloud providers) from the composite into managed resources.</li> <li>For Garage, map <code>spec.providerConfigRef.name</code> to <code>spec.clusterRef.name</code>.</li> </ul>"},{"location":"runbooks/adding-a-new-provider/#4-register-the-composition","title":"4. Register the composition","text":"<p>Edit <code>packages/&lt;product&gt;/src/composition.ts</code> to export the new composition.</p> <p>Edit <code>packages/&lt;product&gt;/src/index.ts</code> to instantiate it during synthesis.</p>"},{"location":"runbooks/adding-a-new-provider/#5-add-tests","title":"5. Add tests","text":"<p>Add snapshot tests in <code>packages/&lt;product&gt;/test/unit/snapshot.test.ts</code> that assert the new composition emits the expected resources and labels.</p>"},{"location":"runbooks/adding-a-new-provider/#6-update-the-consumer-sdk","title":"6. Update the consumer SDK","text":"<p>If the product is exposed through the TypeScript SDK, update the relevant props to accept the new provider and set <code>compositionSelector</code> accordingly.</p>"},{"location":"runbooks/adding-a-new-provider/#7-update-examples-and-docs","title":"7. Update examples and docs","text":"<p>Add or update example claims in <code>examples/</code> and update <code>docs/architecture.md</code>.</p>"},{"location":"runbooks/adding-a-new-provider/#verification","title":"Verification","text":"<pre><code>npm run build\nnpm run test\nnpm run synth\n</code></pre> <p>For products with the Crossplane CLI installed:</p> <pre><code>npm run validate\n</code></pre>"},{"location":"runbooks/adding-a-new-provider/#see-also","title":"See also","text":"<ul> <li>ADR 006: Multi-Provider Compositions</li> <li>Architecture Overview</li> </ul>"},{"location":"runbooks/debugging-stuck-xr/","title":"Debugging a Stuck XR","text":""},{"location":"runbooks/debugging-stuck-xr/#symptoms","title":"Symptoms","text":"<ul> <li><code>kubectl get &lt;xr&gt;</code> shows <code>SYNCED: False</code> or <code>READY: False</code></li> <li>The claim is waiting for the composite resource to become ready</li> </ul>"},{"location":"runbooks/debugging-stuck-xr/#steps","title":"Steps","text":"<ol> <li>Describe the claim:</li> </ol> <pre><code>kubectl describe &lt;claim-kind&gt; &lt;name&gt; -n &lt;namespace&gt;\n</code></pre> <ol> <li>Describe the XR:</li> </ol> <pre><code>kubectl describe &lt;xr-kind&gt; &lt;name&gt;\n</code></pre> <ol> <li>Check events:</li> </ol> <pre><code>kubectl get events --field-selector involvedObject.name=&lt;xr-name&gt;\n</code></pre> <ol> <li>Common causes:</li> <li>Missing Composition Function (<code>function-patch-and-transform</code> not healthy)</li> <li>Missing provider CRDs</li> <li>Invalid patch paths in Composition</li> <li> <p>Missing secrets referenced by ExternalSecret</p> </li> <li> <p>Render locally:</p> </li> </ol> <pre><code>cd packages/&lt;product&gt;\nnpm run validate\n</code></pre>"},{"location":"runbooks/manual-resource-cleanup/","title":"Manual Resource Cleanup","text":"<p>If a deletion policy of <code>Orphan</code> leaves resources behind:</p> <ol> <li>Identify orphaned resources by label:</li> </ol> <pre><code>kubectl get managed -l crossplane.io/composite=&lt;xr-name&gt;\n</code></pre> <ol> <li>Delete orphaned resources manually:</li> </ol> <pre><code>kubectl delete &lt;kind&gt; &lt;name&gt;\n</code></pre> <ol> <li> <p>For cloud resources, use the AWS console or CLI to verify cleanup.</p> </li> <li> <p>Update the XR deletion policy before re-creating if cleanup is desired:</p> </li> </ol> <pre><code>spec:\n  deletionPolicy: Delete\n</code></pre>"},{"location":"runbooks/rotating-provider-credentials/","title":"Rotating Provider Credentials","text":"<ol> <li>Update the <code>ProviderConfig</code> secret in the target cluster.</li> <li>Verify provider pods restart and reconnect:</li> </ol> <pre><code>kubectl rollout restart deployment -n crossplane-system\n</code></pre> <ol> <li>Check provider health:</li> </ol> <pre><code>kubectl get providers.pkg.crossplane.io\n</code></pre> <ol> <li>Trigger reconciliation of affected XRs:</li> </ol> <pre><code>kubectl annotate &lt;xr-kind&gt; &lt;xr-name&gt; crossplane.io/paused=false --overwrite\n</code></pre>"},{"location":"runbooks/upgrading-composition/","title":"Upgrading a Composition","text":"<ol> <li>Make changes in the CDK8s source.</li> <li>Run tests and validation:</li> </ol> <pre><code>make test\nmake synth\nmake validate\n</code></pre> <ol> <li>Apply the new Composition:</li> </ol> <pre><code>kubectl apply -f packages/&lt;product&gt;/dist/composition.k8s.yaml\n</code></pre> <ol> <li>Verify existing XRs reconcile:</li> </ol> <pre><code>kubectl get &lt;xr-kind&gt; -w\n</code></pre> <ol> <li>Rollback if needed:</li> </ol> <pre><code>kubectl apply -f packages/&lt;product&gt;/dist/composition.k8s.yaml  # previous version\n</code></pre>"}]}